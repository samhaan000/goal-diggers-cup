const goalEventsKey="goal-diggers-cup-2026-goals";
function readGoalEvents(){try{return JSON.parse(localStorage.getItem(goalEventsKey))||{}}catch{return {}}}
function goalListForMatch(matchId){const data=readGoalEvents();return Object.values(data[String(matchId)]||{}).sort((a,b)=>(a.createdAt||"").localeCompare(b.createdAt||""));}
function playerDisplay(g){return `#${g.playerNumber} ${g.playerName}${g.ownGoal?" (OG)":""}`;}
function minuteDisplay(g){return g.matchTime||"";}
function goalLine(g){return `<span>${playerDisplay(g)}</span><b>${minuteDisplay(g)}</b>`}
function renderGoalsUnderLive(match){let wrap=document.getElementById("liveGoalScorers");const board=document.getElementById("liveMatchBoard");if(!board)return;if(!wrap){wrap=document.createElement("div");wrap.id="liveGoalScorers";wrap.className="goal-scorers live-goal-scorers";board.appendChild(wrap)}if(!match){wrap.innerHTML="";return}const goals=goalListForMatch(match.id);const home=goals.filter(g=>g.side==="home");const away=goals.filter(g=>g.side==="away");wrap.innerHTML=goals.length?`<div class="goal-side goal-side-home">${home.map(goalLine).join("")}</div><div class="goal-side goal-side-away">${away.map(goalLine).join("")}</div>`:"";}
function renderGoalTimelines(){document.querySelectorAll(".match-card").forEach(card=>{const meta=card.querySelector(".match-meta")?.textContent||"";const idMatch=meta.match(/#(\d+)/);if(!idMatch)return;const id=idMatch[1];let box=card.querySelector(".match-goals");if(!box){box=document.createElement("div");box.className="match-goals";card.appendChild(box)}const goals=goalListForMatch(id);box.innerHTML=goals.length?goals.map(g=>`<span>${playerDisplay(g)} ${minuteDisplay(g)}</span>`).join(""):""})}
function renderStatsTab(){const panel=document.getElementById("panel-stats");if(!panel)return;const all=Object.values(readGoalEvents()).flatMap(x=>Object.values(x||{}));const scorerMap={};const ownMap={};all.forEach(g=>{const key=g.playerId||`${g.playerTeam}_${g.playerNumber}_${g.playerName}`;if(g.ownGoal){ownMap[key]=ownMap[key]||{...g,goals:0};ownMap[key].goals++}else{scorerMap[key]=scorerMap[key]||{...g,goals:0};scorerMap[key].goals++}});const scorers=Object.values(scorerMap).sort((a,b)=>b.goals-a.goals||a.playerName.localeCompare(b.playerName));const ownGoals=Object.values(ownMap).sort((a,b)=>b.goals-a.goals||a.playerName.localeCompare(b.playerName));panel.innerHTML=`<div class="section-head"><div><span class="section-kicker">Stats</span><h2>Top Scorers</h2></div><p>Own goals are not counted for Golden Boot.</p></div><div class="stats-list">${scorers.length?scorers.map((p,i)=>`<div class="stats-row"><span>${i+1}</span><strong>#${p.playerNumber} ${p.playerName}</strong><small>${p.playerTeam}</small><b>${p.goals}</b></div>`).join(""):`<div class="empty-state">No goals recorded yet.</div>`}</div><div class="section-head own-goal-head"><div><span class="section-kicker">Own Goals</span><h2>OG Tracker</h2></div></div><div class="stats-list own-goal-list">${ownGoals.length?ownGoals.map((p,i)=>`<div class="stats-row"><span>${i+1}</span><strong>#${p.playerNumber} ${p.playerName}</strong><small>${p.playerTeam}</small><b>${p.goals}</b></div>`).join(""):`<div class="empty-state">No own goals recorded.</div>`}</div>`;}
function refreshGoalDisplays(){try{renderGoalsUnderLive(typeof liveMatch==="function"?liveMatch():null);renderGoalTimelines();renderStatsTab()}catch(e){console.warn("Goal display refresh failed",e)}}
const oldRenderMatchesForGoals=renderMatches;renderMatches=function(){oldRenderMatchesForGoals();refreshGoalDisplays()};
const oldRenderTableForGoals=renderTable;renderTable=function(){oldRenderTableForGoals();refreshGoalDisplays()};
function loadGoalEventsFromFirebase(){if(!window.goalDiggersDb)return;window.goalDiggersDb.ref("goals").on("value",snap=>{localStorage.setItem(goalEventsKey,JSON.stringify(snap.val()||{}));refreshGoalDisplays()})}
setTimeout(loadGoalEventsFromFirebase,1800);
setInterval(refreshGoalDisplays,2000);
refreshGoalDisplays();